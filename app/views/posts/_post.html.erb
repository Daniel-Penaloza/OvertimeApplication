<tr>
	<td><%= post.id %></td>
	<td><%= post.date %></td>
	<td><%= post.user.full_name %></td>
	<td><%= post.rationale %></td>
	<td><%= link_to "Edits", edit_post_path(post), id: "edit_#{post.id}_post" %></td>
</tr>
