<%= form_for @post do |f| %>
	<%= f.label :date %>
	<%= f.date_field :date %>

	<%= f.label :rationale %>
	<%= f.text_area :rationale %>

	<%= f.submit if @post.new_record? ? "Create Post" : "Update Post" %>
<% end %>